# ============================================
# Voice Service Docker Ignore 配置
# ============================================
# 注意：构建上下文是项目根目录，所以路径是相对于项目根目录的
# 只排除模型文件目录，避免重复复制

# 模型文件目录（重要：避免重复复制）
# 模型文件已在 Dockerfile 中单独复制到 /workspace/models/
# 这里排除代码目录中的模型，避免镜像中保留两份
services/voice-service/app/services/models/